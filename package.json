{
  "name": "ci-cd-demo",
  "version": "1.0.0",
  "description": "CI/CD demo project with GitHub Actions for interns",
  "private": true,
  "scripts": {
    "lint": "eslint \"*.js\"",
    "test": "jest --coverage",
    "build": "node build.js"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  }
}
